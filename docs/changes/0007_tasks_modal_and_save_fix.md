[FIX] 2026-02-09 任务创建改为模态框并修复保存参数封装（local）

## 当前进度

- 已完成：任务页“新建任务”入口改为弹出式模态框承载创建/编辑表单。
- 已完成：模态框打开时锁定 `body` 滚动，避免出现全局滚动条。
- 已完成：修复任务创建/更新调用参数封装，解决“任务保存失败”。

## 改动记录（最近）

- `apps/desktop/src/pages/tasks.tsx`：
  - 移除页面内联展开表单，改为固定定位遮罩 + `MagicCard` 模态承载原有表单内容。
  - 新建按钮改为始终打开模态框（保留原有创建/编辑表单字段与提交逻辑）。
  - 支持点击遮罩关闭模态框（无右上角关闭按钮）。
  - 新增模态开关的 `body` 滚动锁定逻辑，避免全局页面滚动条闪现。
- `apps/desktop/src/data/tauri-client.ts`：
  - `createTask` 与 `updateTask` 调用改为传入 `{ input: { spec } }`，对齐 Tauri 命令参数结构。

## 未来发展（优先级）

1. 高：补充桌面端任务页 e2e，用例覆盖“新建任务弹窗打开/关闭 + 成功保存 + 失败提示”。
2. 中：统一任务相关 Tauri 调用的参数传递方式，降低后续命令签名演进风险。
3. 中：为任务创建弹窗增加键盘可达性细节（例如焦点初始定位/恢复策略）。
