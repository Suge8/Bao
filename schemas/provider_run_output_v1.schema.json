{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bao.provider.run.output/v1",
  "type": "object",
  "required": ["kind"],
  "properties": {
    "kind": { "type": "string", "enum": ["message", "tool_call", "tool_calls"] },
    "message": { "type": "string" },
    "toolCall": { "$ref": "bao.toolcall.ir/v1" },
    "toolCalls": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "bao.toolcall.ir/v1" }
    }
  },
  "allOf": [
    { "if": { "properties": { "kind": { "const": "message" } } }, "then": { "required": ["message"] } },
    { "if": { "properties": { "kind": { "const": "tool_call" } } }, "then": { "required": ["toolCall"] } },
    { "if": { "properties": { "kind": { "const": "tool_calls" } } }, "then": { "required": ["toolCalls"] } }
  ]
}
