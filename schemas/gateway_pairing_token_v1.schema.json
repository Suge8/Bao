{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "bao.gateway.pairing_token/v1",
  "type": "object",
  "required": ["token", "kind"],
  "properties": {
    "token": { "type": "string", "minLength": 1 },
    "kind": { "type": "string", "enum": ["pairing", "device"] }
  }
}
